new page
<%= form_with(model:@feed, local:true) do | form | %>
  <div class = "feeds_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit "submit" %>
<% end %>